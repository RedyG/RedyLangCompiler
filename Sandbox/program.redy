mod test;

type Person = struct {
  age: i32,
  height: i32,
};

fn main() {
  var person = Person { age: 20, height: 200 };
  modify(&person.height);
  person.height
}

fn modify(a: &i32) {
  *a = 10
}